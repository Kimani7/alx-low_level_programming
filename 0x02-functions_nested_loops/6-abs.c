#include "main.h"
/**
 * _abs - computes the absolute value
 * of an interger
 * @int: input as an interger
 * Return: absolute value
 */

int _abs(int)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
