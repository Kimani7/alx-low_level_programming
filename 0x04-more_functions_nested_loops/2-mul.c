#include "main.h"
/**
 * mul - a functuion that multiplies two intergers
 * @a: first interger
 * @b: second interger
 * Return: multiplacation
 */

int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}
