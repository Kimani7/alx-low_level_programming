#include "main.h"
/**
 * print_diagonal - draws a diagonal line on terminal
 * @n: interger
 * Return: always (0)
 */
void print_diagonal(int n)
{
	int k;

	for (k = 0; k < n; k++)
	{
		if (n > 0)
		{
		_putchar('\');
		}
	}
	_putchar('\n');
}
